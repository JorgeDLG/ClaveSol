@model ClaveSol.Models.SubCategory

@{
    ViewData["Title"] = "Detalles";
}

<h1>@Html.DisplayFor(model => model.Name)</h1>
@* <h1>Detalles</h1> *@

@* <div>
    <h4>SubCategoria</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Category)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
    </dl>
</div> *@

<div class="row row-cols-1 row-cols-md-3">
@foreach (var ins in ViewBag.instruments)
{
      <div class="col mb-4" style="max-width:20em;">
        <div class="card h-100" >
          <img src="~/assets/CatSubCatImage/SubCats/defaultSubCat.jpg" class="card-img-top">
          <div class="card-body text-center">
            @* <h2 class="card-title text-center">@subCat.Name</h2> *@
            <a class="card-title" asp-asp-area="" asp-controller="Instruments" asp-action="Details" asp-route-id="@ins.Id" style="font-size:1.5em;">@ins.Name</a>
          </div>
        </div>
      </div>
}
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Volver al listado</a>
</div>
